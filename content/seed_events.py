# Content team — database seeder
# Read events.csv and load every row into the SQLite database
# Run with: python content/seed_events.py
# Verify by opening content/events.db in DB Browser for SQLite
